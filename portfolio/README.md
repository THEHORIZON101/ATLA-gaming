# UMBR-A Portfolio

A responsive static portfolio highlighting selected web applications, browser games, interface work, testing, and debugging skills.

## Preview locally

Open `index.html` directly or run a static server:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000`.

## Publish

This project is ready for GitHub Pages and does not require a build step.
